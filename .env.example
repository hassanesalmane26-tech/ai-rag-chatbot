# Safe placeholders only. Copy locally; never commit real values.
TRIDENT_ENV=production
TRIDENT_APP_NAME=TRIDENT AI
TRIDENT_APP_VERSION=1.0.0-rc
TRIDENT_BUILD_SHA=replace-with-deployed-git-sha
TRIDENT_DEBUG=false
TRIDENT_LOG_LEVEL=INFO
TRIDENT_DATABASE_URL=postgresql://trident:replace-locally@127.0.0.1:5432/trident
TRIDENT_DOCUMENTS_PATH=/absolute/path/to/documents/workspaces
TRIDENT_VECTOR_DB_PATH=/absolute/path/to/vector_db
TRIDENT_SECURITY_MODE=oidc

# Required when TRIDENT_SECURITY_MODE=oidc. The issuer must use HTTPS.
TRIDENT_OIDC_ISSUER=https://identity.example.com
TRIDENT_OIDC_AUDIENCE=trident-api
TRIDENT_OIDC_CLIENT_ID=replace-with-public-client-id
TRIDENT_OIDC_REDIRECT_URI=https://app.example.com/api/v1/session/callback
TRIDENT_OIDC_POST_LOGOUT_REDIRECT_URI=https://app.example.com/
TRIDENT_OIDC_SCOPES=openid profile
# Optional: omit to use issuer discovery.
TRIDENT_OIDC_JWKS_URL=
TRIDENT_OIDC_ALLOWED_ALGORITHMS=RS256
TRIDENT_OIDC_CLOCK_SKEW_SECONDS=30
TRIDENT_OIDC_HTTP_TIMEOUT_SECONDS=5
TRIDENT_SESSION_TTL_SECONDS=28800
TRIDENT_LOGIN_TRANSACTION_TTL_SECONDS=600
TRIDENT_SESSION_COOKIE_SECURE=true
TRIDENT_RATE_LIMIT_REQUESTS_PER_MINUTE=300

# Comma-separated exact browser origins. Wildcards are rejected.
# Leave empty for the recommended same-origin Nginx deployment. If a distinct
# browser origin is approved, list its exact HTTPS origin here.
TRIDENT_CORS_ALLOWED_ORIGINS=

# Provider credential used only by the backend AI adapter; never expose it as VITE_*.
TRIDENT_OPENAI_API_KEY=replace-in-runtime-secret-store
TRIDENT_OPENAI_CHAT_MODEL=gpt-4.1-mini
TRIDENT_OPENAI_EMBEDDING_MODEL=text-embedding-3-small
TRIDENT_PROVIDER_TIMEOUT_SECONDS=60
